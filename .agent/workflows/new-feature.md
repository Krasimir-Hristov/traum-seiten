---
description: Create a new feature following strict structural guidelines
---
1. Create a new feature directory in `src/features/`.
2. Define the `types.ts` (Frontend) and `schemas.py` (Backend) representing the domain models.
3. Implement the Backend Logic (FastAPI).
4. Implement the Frontend UI using Stitch MCP, strictly adhering to the Premium Design constraint.
