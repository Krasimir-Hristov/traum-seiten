// src/lib/constants.ts
// SSoT for Frontend API endpoints and global routes.

export const API_ROUTES = {
  // e.g. BOOKS: '/api/v1/books',
};

export const GLOBAL_SETTINGS = {
  // e.g. APP_NAME: "Traumseiten",
};
